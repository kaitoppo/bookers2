<h1>Book detail</h1>
<table>
    <thead>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th colspan="3"></th>
    </thead>
    <tbody>
      <td><%= image_tag @book.user.get_profile_image(100,100) %>
      <br>
      <%= link_to user_path(@book.user.id) do %>
      <%= @book.user.name %>
      <% end %>
      </td>
      <td>
       <%= link_to book_path(@book.id) do %>
       <%= @book.title %> 
       <% end %>
      </td>
      <td><%= @book.body %></td>
      <th colspan="3"></th>
      <% if @book.user == current_user %>
      <td><%= link_to "Edit", edit_book_path(@book) %></td>
      <td><%= link_to "Destroy", books_path, metod: :delete, "data-confirm" => "本当に削除しますか？" %></td>
       <% end %>
    </tbody>
</table>
    </tb>
